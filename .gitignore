# Rust build output (regenerated by cargo; never commit)
# Unanchored so each crate's build dir (agent/target/, collector/target/) is covered.
target/

# rustfmt backups
**/*.rs.bk

# MSVC debug symbols
*.pdb
archive/
*.duckdb

# Local investigation notes (not part of the codebase)
troubleshooting/
